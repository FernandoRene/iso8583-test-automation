# Cucumber Configuration
# ? CONFIGURACIÓN FINAL - PicoFactory mejorado

# ObjectFactory personalizado
# cucumber.object-factory=com.iso8583.test.config.PicoFactory

# Opciones de ejecución
cucumber.execution.parallel.enabled=false
cucumber.execution.parallel.config.fixed.parallelism=1
cucumber.publish.quiet=true
cucumber.publish.enabled=false

# Formato de snippets
cucumber.snippet-type=camelcase

# Configuración específica para PicoContainer
cucumber.pico-container-context=singleton

# Logging (opcional)
cucumber.plugin=pretty, html:target/cucumber-reports/cucumber.html