# ============================================================================
# ALLURE ENVIRONMENT PROPERTIES
# Este archivo se genera automáticamente durante la ejecución de tests
# ============================================================================

# Project Information
project.name=ISO8583 Test Automation Framework
project.version=1.0.0-SNAPSHOT
project.type=Proyecto de Grado - Ingeniería de Sistemas

# University Information
university.name=Universidad Unión Bolivariana
university.department=Carrera de Ingeniería de Sistemas
project.author=Fernando Rene Llusco Blanco

# Test Environment
test.environment=${env.TEST_ENV:-LOCAL}
test.profile=${env.TEST_PROFILE:-test}
test.execution.date=${timestamp}

# Simulator Configuration
simulator.url=${simulator.url:-http://localhost:8081}
simulator.mode=${simulator.mode:-REAL}
simulator.target.host=172.16.1.211
simulator.target.port=5105

# Technology Stack
java.version=${java.version}
cucumber.version=7.15.0
junit.version=5.10.1
allure.version=2.25.0
rest-assured.version=5.4.0

# Framework Configuration
framework.type=Cucumber + JUnit 5 Platform
dependency.injection=PicoContainer
reporting.tools=Allure + Maven Surefire
api.client=RestAssured

# Test Execution Settings
test.timeout=30000
test.max.retries=3
test.parallel.enabled=false

# Operating System
os.name=${os.name}
os.version=${os.version}
os.arch=${os.arch}

# Build Information
build.timestamp=${timestamp}
maven.version=${maven.version}